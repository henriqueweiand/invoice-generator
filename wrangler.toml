# Cloudflare Pages configuration
# This file is used when deploying with `wrangler pages deploy`
# For dashboard deploys, set these values in the Cloudflare Pages UI instead.

name = "my-app"
pages_build_output_dir = "out"

# Uncomment to add environment variables:
# [vars]
# MY_VAR = "value"
